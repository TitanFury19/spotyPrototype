import { NoimagepipePipe } from './noimagepipe.pipe';

describe('NoimagepipePipe', () => {
  it('create an instance', () => {
    const pipe = new NoimagepipePipe();
    expect(pipe).toBeTruthy();
  });
});
